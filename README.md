# gnome-macos-buttons
macOS-style window buttons for GNOME. Replaces close, minimize, and maximize buttons with colored circles that reveal symbols on hover. Works with any GTK theme via CSS overrides. GTK3 &amp; GTK4.
